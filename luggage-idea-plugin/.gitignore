.gradle/
build/
out/
*.iml
.idea/
gradle/wrapper/gradle-wrapper.jar

# generated by syncSharedAssets — written into build/generated/sharedAssets,
# so already covered by build/ above. These legacy paths stay listed in case
# you ever run an old branch that wrote them in-tree.
src/main/resources/schemas/
src/main/resources/types/
